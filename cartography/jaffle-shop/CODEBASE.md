# CODEBASE.md — Brownfield Cartographer Living Context

> Auto-generated by Brownfield Cartographer on 2026-03-13 20:42 UTC
> Repository: `D:\Projects\jaffle-shop`

## Architecture Overview

This codebase contains 33 modules (18 yaml, 15 sql), 21 datasets, and 15 data transformations. The import graph has 15 edges with 0 circular dependencies.

**Domain Architecture:**
- `config` (2 files): `.pre-commit-config.yaml`, `dbt_project.yml`
- `ingestion` (1 files): `models/staging/__sources.yml`
- `serving` (13 files): `models/marts/customers.sql`, `models/marts/customers.yml`, `models/marts/locations.sql`
- `staging` (12 files): `models/staging/stg_customers.sql`, `models/staging/stg_customers.yml`, `models/staging/stg_locations.sql`
- `utilities` (2 files): `macros/cents_to_dollars.sql`, `macros/generate_schema_name.sql`

## Critical Path (Top Modules by PageRank)

These are the most imported/depended-upon modules. Changes here have the highest blast radius.

- `macros/cents_to_dollars.sql` (PageRank=0.0312, imported_by=1)
  - This file provides a standardized utility for converting monetary values stored 
- `macros/generate_schema_name.sql` (PageRank=0.0312, imported_by=1)
  - This macro dynamically determines the database schema where dbt models, seeds, a
- `models/marts/customers.sql` (PageRank=0.0312, imported_by=1)
  - This mart enriches customer data with lifetime purchase metrics and behavioral c
- `models/marts/locations.sql` (PageRank=0.0312, imported_by=1)
  - This file creates a refined business-ready dataset of location information by pr
- `models/marts/metricflow_time_spine.sql` (PageRank=0.0312, imported_by=1)
  - This file creates a continuous calendar table spanning a 10-year period to serve
- `models/marts/order_items.sql` (PageRank=0.0312, imported_by=1)
  - This mart table enriches order line items with comprehensive context by combinin
- `models/marts/orders.sql` (PageRank=0.0312, imported_by=1)
  - This mart aggregates order data with item-level details, computing order totals,
- `models/marts/products.sql` (PageRank=0.0312, imported_by=1)
  - This mart table provides a cleaned and business-ready products dataset for downs
- `models/marts/supplies.sql` (PageRank=0.0312, imported_by=1)
  - This file creates a cleaned and business-ready supplies dataset by transforming 
- `models/staging/stg_customers.sql` (PageRank=0.0312, imported_by=1)
  - This file transforms raw customer data into a clean, standardized staging layer 

## Data Sources & Sinks

**Data Sources (ingestion points):**
- `raw_customers`
- `raw_items`
- `raw_orders`
- `raw_products`
- `raw_stores`
- `raw_supplies`

**Data Sinks (output datasets):**
- `cents_to_dollars`
- `customers`
- `generate_schema_name`
- `locations`
- `metricflow_time_spine`
- `products`
- `supplies`

## Known Debt

**Circular Dependencies:** None detected.

**Documentation Drift:** None detected.

## High-Velocity Files (likely pain points)

Files changing most frequently in the last 30 days:

- No recent git activity detected.

## Module Purpose Index

| File | Language | Domain | Purpose |
|------|----------|--------|---------|
| `.pre-commit-config.yaml` | yaml | config | This configuration file enforces code quality and consistenc |
| `Taskfile.yml` | yaml | — | This file orchestrates the setup and initialization of a jaf |
| `dbt_project.yml` | yaml | config | This configuration file establishes the foundational setting |
| `macros/cents_to_dollars.sql` | sql | utilities | This file provides a standardized utility for converting mon |
| `macros/generate_schema_name.sql` | sql | utilities | This macro dynamically determines the database schema where  |
| `models/marts/customers.sql` | sql | serving | This mart enriches customer data with lifetime purchase metr |
| `models/marts/customers.yml` | yaml | serving | This file defines a comprehensive customer data mart that ag |
| `models/marts/locations.sql` | sql | serving | This file creates a refined business-ready dataset of locati |
| `models/marts/locations.yml` | yaml | serving | This file defines a semantic layer for location data that en |
| `models/marts/metricflow_time_spine.sql` | sql | serving | This file creates a continuous calendar table spanning a 10- |
| `models/marts/order_items.sql` | sql | serving | This mart table enriches order line items with comprehensive |
| `models/marts/order_items.yml` | yaml | serving | This file defines the order items data mart—a business-ready |
| `models/marts/orders.sql` | sql | serving | This mart aggregates order data with item-level details, com |
| `models/marts/orders.yml` | yaml | serving | This file defines a comprehensive order data mart that aggre |
| `models/marts/products.sql` | sql | serving | This mart table provides a cleaned and business-ready produc |
| `models/marts/products.yml` | yaml | serving | This file defines the product dimension layer for semantic a |
| `models/marts/supplies.sql` | sql | serving | This file creates a cleaned and business-ready supplies data |
| `models/marts/supplies.yml` | yaml | serving | This file defines the structure and attributes of supply inv |
| `models/staging/__sources.yml` | yaml | ingestion | This file documents the raw data sources that feed the Jaffl |
| `models/staging/stg_customers.sql` | sql | staging | This file transforms raw customer data into a clean, standar |
| `models/staging/stg_customers.yml` | yaml | staging | This file defines the structure and quality expectations for |
| `models/staging/stg_locations.sql` | sql | staging | This staging model standardizes and prepares raw store locat |
| `models/staging/stg_locations.yml` | yaml | staging | This file defines a cleaned and standardized locations datas |
| `models/staging/stg_order_items.sql` | sql | staging | This model transforms raw e-commerce item transaction data i |
| `models/staging/stg_order_items.yml` | yaml | staging | This file defines the data quality rules and documentation s |
| `models/staging/stg_orders.sql` | sql | staging | This model cleanly transforms raw e-commerce order data into |
| `models/staging/stg_orders.yml` | yaml | staging | This file defines the quality standards and metadata for a c |
| `models/staging/stg_products.sql` | sql | staging | This file standardizes and enriches raw product data by rena |
| `models/staging/stg_products.yml` | yaml | staging | This file defines the structure and quality standards for a  |
| `models/staging/stg_supplies.sql` | sql | staging | This file prepares raw supply inventory data for downstream  |
| `models/staging/stg_supplies.yml` | yaml | staging | This file defines the structure and quality requirements for |
| `package-lock.yml` | yaml | — | This file locks specific versions of dbt packages and extern |
| `packages.yml` | yaml | — | This file declares external dependencies that extend dbt's c |

## FDE Day-One Answers

### Primary Data Ingestion Path
Raw data enters the pipeline through six source tables defined in models/staging/__sources.yml: raw_customers, raw_orders, raw_items, raw_stores, raw_products, and raw_supplies. These sources flow through staging models (e.g., models/staging/stg_customers.sql) configured as views per dbt_project.yml, then materialize as tables in the marts layer (models/marts/). The macros/generate_schema_name.sql controls schema routing across environments, separating seeds into a dedicated schema and prefixing production schemas.

### Most Critical Output Datasets
The 5 most critical output datasets are models/marts/customers.sql, models/marts/orders.sql, models/marts/order_items.sql, models/marts/locations.sql, and models/marts/products.sql. customers.sql is confirmed as a top hub by PageRank and serves as the central customer dimension with lifetime value metrics; orders.sql and order_items.sql form the core transactional fact tables powering revenue and profitability analytics. locations.sql is also a top-PageRank hub, and products.sql anchors product-dimension joins across the organization.

### Blast Radius of Critical Module Failure
If models/marts/customers.sql fails (the highest PageRank mart node), all downstream customer segmentation, lifetime value reporting, and analytics dashboards lose their single source of truth for customer data. Its customers.yml defines reusable metrics and semantic definitions consumed by MetricFlow and BI tools, meaning metric queries and dashboards built on the semantic layer would also break. Additionally, since stg_customers.sql feeds customers.sql, a failure cascades back to raw_customers ingestion validation as well.

### Business Logic: Concentrated vs Distributed
Business logic is heavily concentrated in the marts layer, specifically in models/marts/customers.sql (lifetime metrics, behavioral classification), models/marts/orders.sql (order totals, category counts, boolean flags, sequence numbering), and models/marts/order_items.sql (profitability enrichment). Distributed utility logic lives in macros/cents_to_dollars.sql and macros/generate_schema_name.sql, which are cross-cutting concerns applied pipeline-wide. The staging layer (e.g., models/staging/stg_customers.sql) is intentionally thin, handling only renaming and field selection without substantive business rules.

### High-Velocity Files (Git Activity)
models/marts/customers.yml and models/marts/orders.yml are likely high-velocity pain points because they define both data quality tests and semantic metric definitions, meaning they must change whenever business definitions, KPIs, or segmentation rules evolve. models/marts/order_items.sql and models/marts/orders.sql are also likely frequent change targets as they aggregate financial and categorical metrics that respond to new product types or pricing logic. macros/cents_to_dollars.sql is a high-risk shared dependency since any change to it propagates to all financial calculations across the pipeline.
