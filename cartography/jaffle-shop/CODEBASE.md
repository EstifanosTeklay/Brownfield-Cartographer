# CODEBASE.md — Brownfield Cartographer Living Context

> Auto-generated by Brownfield Cartographer on 2026-03-15 22:30 UTC
> Repository: `D:\Projects\jaffle-shop`

## Architecture Overview

This codebase contains 33 modules (18 yaml, 15 sql), 21 datasets, and 15 data transformations. The import graph has 46 edges with 0 circular dependencies.

**Domain Architecture:**
- `config` (2 files): `.pre-commit-config.yaml`, `dbt_project.yml`
- `ingestion` (1 files): `models/staging/__sources.yml`
- `serving` (13 files): `models/marts/customers.sql`, `models/marts/customers.yml`, `models/marts/locations.sql`
- `staging` (12 files): `models/staging/stg_customers.sql`, `models/staging/stg_customers.yml`, `models/staging/stg_locations.sql`
- `utilities` (2 files): `macros/cents_to_dollars.sql`, `macros/generate_schema_name.sql`

## Critical Path (Top Modules by PageRank)

These are the most imported/depended-upon modules. Changes here have the highest blast radius.

- `models/staging/stg_customers.yml` (PageRank=0.0442, imported_by=2)
  - This file defines the schema and quality standards for the cleaned customer dime
- `models/staging/stg_locations.yml` (PageRank=0.0442, imported_by=2)
  - This file defines a cleaned and standardized location dimension table that trans
- `models/staging/stg_order_items.yml` (PageRank=0.0442, imported_by=2)
  - This file defines data quality expectations and documentation standards for the 
- `models/staging/stg_orders.yml` (PageRank=0.0442, imported_by=2)
  - This file defines the validation rules and metadata for a cleaned order dataset,
- `models/staging/stg_products.yml` (PageRank=0.0442, imported_by=2)
  - This file defines the structure and quality standards for a cleaned product data
- `models/staging/stg_supplies.yml` (PageRank=0.0442, imported_by=2)
  - This file defines the structure and validation rules for cleaned supply expense 
- `models/marts/customers.yml` (PageRank=0.0415, imported_by=2)
  - This file defines a comprehensive customer analytics layer that aggregates order
- `models/marts/locations.yml` (PageRank=0.0415, imported_by=2)
  - This file defines a dimension semantic layer for location data that enables busi
- `models/marts/order_items.yml` (PageRank=0.0415, imported_by=2)
  - This file defines the order items data mart, establishing validation rules to en
- `models/marts/orders.yml` (PageRank=0.0415, imported_by=2)
  - This file defines the order data mart—a customer-facing analytics table that con

## Data Sources & Sinks

**Data Sources (ingestion points):**
- `raw_customers`
- `raw_items`
- `raw_orders`
- `raw_products`
- `raw_stores`
- `raw_supplies`

**Data Sinks (output datasets):**
- `cents_to_dollars`
- `customers`
- `generate_schema_name`
- `locations`
- `metricflow_time_spine`
- `products`
- `supplies`

## Known Debt

**Circular Dependencies:** None detected.

**Documentation Drift:** None detected.

## High-Velocity Files (likely pain points)

Files changing most frequently in the last 30 days:

- No recent git activity detected.

## Module Purpose Index

| File | Language | Domain | Purpose |
|------|----------|--------|---------|
| `.pre-commit-config.yaml` | yaml | config | This file configures automated code quality checks that run  |
| `Taskfile.yml` | yaml | — | This automation orchestrator sets up and executes a complete |
| `dbt_project.yml` | yaml | config | This file establishes the foundational configuration and org |
| `macros/cents_to_dollars.sql` | sql | utilities | This macro standardizes the conversion of monetary values st |
| `macros/generate_schema_name.sql` | sql | utilities | This macro dynamically determines the database schema destin |
| `models/marts/customers.sql` | sql | serving | This mart creates a comprehensive customer analytics table b |
| `models/marts/customers.yml` | yaml | serving | This file defines a comprehensive customer analytics layer t |
| `models/marts/locations.sql` | sql | serving | This mart table aggregates and exposes cleaned location data |
| `models/marts/locations.yml` | yaml | serving | This file defines a dimension semantic layer for location da |
| `models/marts/metricflow_time_spine.sql` | sql | serving | This table creates a continuous sequence of dates spanning a |
| `models/marts/order_items.sql` | sql | serving | This mart table enriches order line items with contextual bu |
| `models/marts/order_items.yml` | yaml | serving | This file defines the order items data mart, establishing va |
| `models/marts/orders.sql` | sql | serving | This mart table constructs a comprehensive order dimension b |
| `models/marts/orders.yml` | yaml | serving | This file defines the order data mart—a customer-facing anal |
| `models/marts/products.sql` | sql | serving | This mart table provides a cleaned and business-ready produc |
| `models/marts/products.yml` | yaml | serving | This file defines the product dimension data structure for a |
| `models/marts/supplies.sql` | sql | serving | This file creates a market-facing supplies table by aggregat |
| `models/marts/supplies.yml` | yaml | serving | This file defines the structure and business context for sup |
| `models/staging/__sources.yml` | yaml | ingestion | This file defines the raw data sources that feed the Jaffle  |
| `models/staging/stg_customers.sql` | sql | staging | This file creates a cleaned and standardized customer dimens |
| `models/staging/stg_customers.yml` | yaml | staging | This file defines the schema and quality standards for the c |
| `models/staging/stg_locations.sql` | sql | staging | This staging model standardizes and prepares raw store locat |
| `models/staging/stg_locations.yml` | yaml | staging | This file defines a cleaned and standardized location dimens |
| `models/staging/stg_order_items.sql` | sql | staging | This model transforms raw e-commerce item-level transaction  |
| `models/staging/stg_order_items.yml` | yaml | staging | This file defines data quality expectations and documentatio |
| `models/staging/stg_orders.sql` | sql | staging | This staging model transforms raw e-commerce order data into |
| `models/staging/stg_orders.yml` | yaml | staging | This file defines the validation rules and metadata for a cl |
| `models/staging/stg_products.sql` | sql | staging | This model transforms raw product data from the source syste |
| `models/staging/stg_products.yml` | yaml | staging | This file defines the structure and quality standards for a  |
| `models/staging/stg_supplies.sql` | sql | staging | This file standardizes and enriches raw supply inventory dat |
| `models/staging/stg_supplies.yml` | yaml | staging | This file defines the structure and validation rules for cle |
| `package-lock.yml` | yaml | — | This file locks specific versions of dbt packages and depend |
| `packages.yml` | yaml | — | This file declares external dbt packages and dependencies th |

## FDE Day-One Answers

### Primary Data Ingestion Path
Raw data enters through six source tables defined in 'models/staging/__sources.yml': raw_customers, raw_orders, raw_items, raw_stores, raw_products, and raw_supplies. These feed into five staging models (stg_customers.sql, stg_orders.sql, stg_order_items.sql, stg_locations.sql, stg_products.sql) which are the top hub modules by PageRank, indicating they are the highest-traffic transformation nodes. The staging layer applies standardization and naming conventions before passing data downstream to the marts layer.

### Most Critical Output Datasets
The most critical output datasets are 'customers', 'orders', and 'order_items' based on their complexity and described downstream dependencies. 'customers' (models/marts/customers.sql) aggregates lifetime metrics and segmentation used for BI and marketing; 'orders' (models/marts/orders.sql) is described as the central fact table for order analytics; and 'order_items' (models/marts/order_items.yml) is described as 'the single source of truth for order item analytics.' Supporting dimensions 'locations' and 'products' round out the critical outputs as reusable dimension tables.

### Blast Radius of Critical Module Failure
The most critical module is 'models/staging/stg_orders.yml' (highest PageRank hub), and its failure would cascade to at minimum 'models/marts/orders.sql', 'models/marts/order_items.sql', and 'models/marts/customers.sql', which all depend on order data. Since 'customers.sql' derives lifetime purchase metrics from orders and 'order_items.sql' enriches order line items, a failure in stg_orders would effectively break the entire transactional analytics surface. This represents 3 of the 7 mart outputs and would disable customer segmentation, order analytics, and product performance reporting simultaneously.

### Business Logic: Concentrated vs Distributed
Business logic is heavily concentrated in the marts layer, particularly in 'models/marts/orders.sql' (aggregates item-level costs, derives food/drink flags), 'models/marts/customers.sql' (calculates LTV and behavioral classifications), and 'models/marts/order_items.sql' (enriches with supply cost and product details). The 'macros/cents_to_dollars.sql' represents distributed reusable logic for monetary conversion used across multiple models. The staging layer (stg_*.sql files) is intentionally thin, handling only renaming and basic cleaning, meaning complex joins and metric derivation are deferred to and concentrated in the marts.

### High-Velocity Files (Git Activity)
The mart definition YAML files ('models/marts/orders.yml', 'models/marts/customers.yml', 'models/marts/order_items.yml') are likely the highest-velocity pain points because they simultaneously own data quality tests, semantic model definitions, and metric declarations—three concerns that evolve at different rates. 'models/staging/__sources.yml' is also likely frequently changed as new raw sources or fields are onboarded, since it is the single registration point for all upstream data assets. 'macros/cents_to_dollars.sql' may be a pain point given its multi-platform database-specific implementations, which require updates whenever a new target platform is added.
